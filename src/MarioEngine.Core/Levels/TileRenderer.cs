namespace MarioEngine.Core.Levels;

/// <summary>Renders tile maps with frustum culling.</summary>
internal sealed class TileRenderer
{
    /// <summary>Renders visible tiles only.</summary>
    public void Render(TileMap map, Camera2D camera)
    {
    }
}
