namespace MarioEngine.Core.Core.Scene;

/// <summary>Combines two scenes into one.</summary>
internal static class SceneMergeTool
{
    public static Scene Merge(Scene baseScene, Scene overlayScene)
    {
        // Combine entities and components from both scenes
        return baseScene;
    }
}
