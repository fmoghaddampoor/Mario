namespace MarioEngine.Core.UI;

/// <summary>Menu transition effects: fades, slides, and particle backgrounds.</summary>
internal sealed class MenuEffects
{
    /// <summary>Current fade progress (0-1).</summary>
    public float FadeProgress { get; private set; }

    /// <summary>Fades the screen in over the given duration.</summary>
    public void FadeIn(float duration)
    {
    }

    /// <summary>Fades the screen out over the given duration.</summary>
    public void FadeOut(float duration)
    {
    }
}
