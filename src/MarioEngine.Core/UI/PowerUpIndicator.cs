namespace MarioEngine.Core.UI;

/// <summary>Shows current power-up icon and remaining duration bar.</summary>
internal sealed class PowerUpIndicator
{
    /// <summary>Updates the indicator with power-up state.</summary>
    public void Update(PowerUpType? type, float durationRemaining, float maxDuration)
    {
    }
}
