namespace MarioEngine.Core.UI;

/// <summary>Audio settings panel with per-bus volume sliders and mute toggles.</summary>
internal sealed class AudioSettingsPanel
{
    /// <summary>Applies current audio settings.</summary>
    public void Apply()
    {
    }
}
